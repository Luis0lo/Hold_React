const Weather = () => {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '80vh',
      }}
    >
      <p style={{ fontSize: '5rem' }}>Weather</p>
    </div>
  );
};

export default Weather;
